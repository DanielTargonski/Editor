add run to the Editor class

this method reads the command and then does the command - moveDown, moveUp,...

the user can quit by typing 'q'

once that is working,

use the new main arguments to pass the filename from the IDE